/**
 * Shared landmark ids. Kept out of `layout.tsx` so the client page can import
 * the id without pulling a server module (and its metadata) into the bundle.
 */
export const MAIN_CONTENT_ID = "main";
